describe('map component functionality', () => {
  test('returns data from the google map API call', () => {
    // mock API call
    // test to confirm data was returned
  });

  test('returns data from the mobile food facility API call', () => {
    // mock API call
    // test to confirm data was returned
  });

  test('renders markers on the map', () => {
    // define markers
    // test if they are truthy
  });

  test('opens the info window upon clicking a marker', () => {
    // define markers and info window
    // mock marker click
    // test if info window is truthy
  });

  test('closes the info window upon clicking the x in the info window', () => {
    // define info window
    // mock close functionality
    // test if info window is falsy
  });
});
