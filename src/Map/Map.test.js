import ReactDOM from 'react-dom';
import Header from './Header/Header';

it('renders markers on the map', () => {
  // define markers
  // test if they are truthy
});

it('opens the info window upon clicking a marker', () => {
  // define markers and info window
  // mock marker click
  // test if info window is truthy
});

it('closes the info window upon clicking the x in the info window', () => {
  // define info window
  // mock close functionality
  // test if info window is falsy
});

it('closes the info window upon clicking outside the info window', () => {
  // define info window
  // mock ouside click
  // test if info window is falsy
});
